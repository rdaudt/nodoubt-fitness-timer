---
phase: 01-foundation-access-and-data-boundaries
plan: "02"
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - middleware.ts
  - .env.example
  - lib/supabase/browser.ts
  - lib/supabase/server.ts
  - lib/supabase/middleware.ts
  - src/features/auth/server/get-auth-context.ts
  - src/features/auth/server/ensure-profile.ts
  - src/features/auth/components/google-sign-in-button.tsx
  - src/features/auth/components/session-boundary.tsx
  - app/auth/callback/route.ts
  - tests/unit/auth-bootstrap.test.ts
  - tests/e2e/phase1-auth-session.spec.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, SETG-02]
user_setup:
  - service: supabase
    why: "Supabase project credentials and Google OAuth provider settings must come from the project dashboard."
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key"
    dashboard_config:
      - task: "Enable Google provider and add local plus deployed redirect URLs"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google"
        details: "Use the callback route introduced in this plan and mirror the same redirect URLs in the Google Cloud OAuth client."
must_haves:
  truths:
    - "Guest users can open the app without being forced through sign-in."
    - "Google sign-in returns to the app with a restorable signed-in session."
    - "Signed-in shell state exposes a first-name header context without using provider metadata as an authorization boundary."
  artifacts:
    - path: "middleware.ts"
      provides: "Session refresh middleware for SSR auth state"
      contains: "updateSession"
    - path: "lib/supabase/server.ts"
      provides: "Server Supabase client for protected server rendering"
      min_lines: 20
    - path: "app/auth/callback/route.ts"
      provides: "OAuth callback exchange route"
      exports: ["GET"]
    - path: "src/features/auth/server/get-auth-context.ts"
      provides: "Trusted auth-context loader for guest versus signed-in rendering"
      min_lines: 25
  key_links:
    - from: "middleware.ts"
      to: "lib/supabase/middleware.ts"
      via: "Next.js middleware session refresh"
      pattern: "updateSession"
    - from: "app/auth/callback/route.ts"
      to: "lib/supabase/server.ts"
      via: "server client code exchange"
      pattern: "createServer"
    - from: "src/features/auth/server/get-auth-context.ts"
      to: "src/features/profiles/contracts/profile.ts"
      via: "profile display mapping"
      pattern: "profile"
---

<objective>
Implement the Supabase Google-auth foundation and signed-in identity context required for the Phase 1 shell.

Purpose: Establish the guest/signed-in boundary as a server-rendered contract with restorable sessions and display-safe first-name context.
Output: Supabase SSR utilities, middleware refresh flow, Google callback route, auth-context loader, profile bootstrap, and auth smoke coverage.
</objective>

<execution_context>
@C:/Users/Carboteiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Carboteiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-access-and-data-boundaries/01-03-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Supabase SSR clients, middleware refresh, and environment contracts</name>
  <files>middleware.ts, .env.example, lib/supabase/browser.ts, lib/supabase/server.ts, lib/supabase/middleware.ts</files>
  <action>Implement the current `@supabase/ssr` browser, server, and middleware client factories and wire middleware so returning sessions refresh through cookies before server rendering. Document the required public env vars in `.env.example`. Keep the auth trust boundary on the server path; do not design the shell around client-memory session detection only.</action>
  <verify>npm run test:unit -- auth-bootstrap</verify>
  <done>Supabase SSR helpers exist, middleware refresh is wired, and local setup requirements are explicit for execution.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Google sign-in exchange and signed-in identity context</name>
  <files>src/features/auth/server/get-auth-context.ts, src/features/auth/server/ensure-profile.ts, src/features/auth/components/google-sign-in-button.tsx, src/features/auth/components/session-boundary.tsx, app/auth/callback/route.ts</files>
  <action>Create the Google sign-in entry component, OAuth callback exchange route, and a server auth-context loader that returns guest or signed-in state plus first-name display data. When the signed-in user lacks a usable profile row, bootstrap the minimal profile record needed for the header chip. Treat provider metadata as display input only; authorization stays tied to Supabase auth identity and the Phase 1 data contracts.</action>
  <verify>npm run test:unit -- auth-bootstrap</verify>
  <done>Guest and signed-in auth states are available to server-rendered routes, the Google callback path is defined, and signed-in identity includes first-name context for the header chip.</done>
</task>

<task type="auto">
  <name>Task 3: Add auth session persistence coverage for return visits and shell rendering</name>
  <files>tests/unit/auth-bootstrap.test.ts, tests/e2e/phase1-auth-session.spec.ts</files>
  <action>Add fast unit coverage for the auth-context loader and Playwright coverage for the login callback/session-restore contract. The test scenarios must prove that guests remain unblocked, signed-in users return to the shell already authenticated after refresh, and the signed-in header chip has first-name context when profile data exists or is derived safely.</action>
  <verify>npm run test:phase1 -- auth-session</verify>
  <done>Phase 1 auth tests fail if SSR session refresh, callback exchange, or signed-in header identity context regresses.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- auth-bootstrap`
- [ ] `npm run test:phase1 -- auth-session`
- [ ] Google provider redirect URLs and local env requirements are documented in `.env.example`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Guests remain unblocked while signed-in users restore correctly on return visits
- The shell can consume a trusted first-name identity context without coupling authorization to provider metadata
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-access-and-data-boundaries/01-02-SUMMARY.md`
</output>
