---
phase: 01-foundation-access-and-data-boundaries
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - playwright.config.ts
  - tests/setup.ts
  - tests/unit/rls-boundary.test.ts
  - tests/integration/ownership-boundary.test.ts
  - src/features/profiles/contracts/profile.ts
  - src/features/templates/contracts/official-template.ts
  - src/features/timers/contracts/timer-record.ts
  - src/features/templates/repositories/official-templates.ts
  - src/features/timers/repositories/personal-timers.ts
  - supabase/migrations/202604150001_phase1_foundation.sql
  - supabase/seed.sql
autonomous: true
requirements: [PRIV-01]
must_haves:
  truths:
    - "Official starter templates are modeled as a public read path separate from private personal timer ownership."
    - "Authenticated timer and draft reads or writes are scoped to the signed-in owner and fail closed when no authenticated owner exists."
    - "Phase 1 has working test commands for unit and integration checks instead of deferring all verification to later phases."
  artifacts:
    - path: "supabase/migrations/202604150001_phase1_foundation.sql"
      provides: "Initial Phase 1 schema and row-level-security policies"
      contains: "enable row level security"
    - path: "src/features/timers/contracts/timer-record.ts"
      provides: "Canonical personal-timer and draft record contract"
      min_lines: 25
    - path: "src/features/templates/contracts/official-template.ts"
      provides: "Official template contract separate from personal ownership"
      min_lines: 20
    - path: "vitest.config.ts"
      provides: "Phase 1 unit and integration test runner configuration"
      min_lines: 10
  key_links:
    - from: "src/features/timers/repositories/personal-timers.ts"
      to: "supabase/migrations/202604150001_phase1_foundation.sql"
      via: "repository assumptions match owner-scoped schema"
      pattern: "owner"
    - from: "src/features/templates/repositories/official-templates.ts"
      to: "src/features/templates/contracts/official-template.ts"
      via: "typed official template reads"
      pattern: "OfficialTemplate"
    - from: "tests/integration/ownership-boundary.test.ts"
      to: "supabase/migrations/202604150001_phase1_foundation.sql"
      via: "integration assertions against RLS contract"
      pattern: "policy|rls|owner"
---

<objective>
Freeze the initial timer/template/profile contracts and Supabase ownership boundary that all later feature work will rely on.

Purpose: Establish Phase 1 as a secure foundation instead of letting UI work race ahead of schema, RLS, and verification.
Output: Supabase migration, seed data, typed repository/contracts, and the Phase 1 unit/integration test scaffold.
</objective>

<execution_context>
@C:/Users/Carboteiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Carboteiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Establish Phase 1 contracts and test harness for a pre-implementation repo</name>
  <files>package.json, vitest.config.ts, playwright.config.ts, tests/setup.ts, src/features/profiles/contracts/profile.ts, src/features/templates/contracts/official-template.ts, src/features/timers/contracts/timer-record.ts</files>
  <action>Add the minimum package scripts and Phase 1 test configuration needed for unit, integration, and browser smoke checks, then define the canonical TypeScript contracts for profile display data, official starter templates, and owner-scoped personal timers/drafts. Keep the contracts narrow and future-proof for Phase 2 authoring work: official templates stay immutable/public, personal timers carry owner identity plus draft status, and profile data stays limited to display concerns.</action>
  <verify>npm run test:unit -- rls-boundary</verify>
  <done>Phase 1 has real test commands and shared contracts that later plans can import without redefining ownership or template shape.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Supabase schema, seed path, and RLS-aware repository scaffolding</name>
  <files>src/features/templates/repositories/official-templates.ts, src/features/timers/repositories/personal-timers.ts, supabase/migrations/202604150001_phase1_foundation.sql, supabase/seed.sql</files>
  <action>Create an initial migration that introduces separate official-template, personal-timer, and profile tables or views, enables RLS on every exposed private table, and defines owner-based read/write policies for authenticated users only. Seed a small official template set for guest browsing and add repository helpers that mirror the schema split so later plans do not collapse public templates into private user space. Make the duplication-before-edit rule explicit in comments or contracts even though editing ships in Phase 2.</action>
  <verify>npm run test:phase1 -- ownership-boundary</verify>
  <done>The schema and repositories clearly separate public starter templates from private owner-scoped timers, and RLS policy intent is executable rather than implied.</done>
</task>

<task type="auto">
  <name>Task 3: Add ownership-boundary tests that fail on cross-user leakage</name>
  <files>tests/unit/rls-boundary.test.ts, tests/integration/ownership-boundary.test.ts</files>
  <action>Add unit tests around repository guards and integration-style assertions around the schema and policy contract so Phase 1 catches the highest-risk regression early: another user's timers or drafts leaking through the data layer. Include an unauthenticated case that proves private reads fail closed when `auth.uid()` is absent.</action>
  <verify>npm run test:unit -- rls-boundary && npm run test:phase1 -- ownership-boundary</verify>
  <done>Tests prove the Phase 1 private data boundary and establish a reusable verification base for later plans.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- rls-boundary`
- [ ] `npm run test:phase1 -- ownership-boundary`
- [ ] Official template seed data exists and is separate from owner-scoped personal timer persistence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Private timer ownership is enforced in schema and repository contracts before UI features build on top of them
- Phase 1 test scaffolding exists early enough for later plans to add coverage instead of inventing it at the end
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-access-and-data-boundaries/01-03-SUMMARY.md`
</output>
