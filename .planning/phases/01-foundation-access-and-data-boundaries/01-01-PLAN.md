---
phase: 01-foundation-access-and-data-boundaries
plan: "01"
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - app/layout.tsx
  - app/(shell)/layout.tsx
  - app/(shell)/page.tsx
  - app/(shell)/templates/page.tsx
  - app/run/layout.tsx
  - components/shell/mobile-shell.tsx
  - components/home/home-screen.tsx
  - components/home/my-timers-section.tsx
  - components/home/official-templates-section.tsx
  - components/header/profile-chip.tsx
  - src/features/home/server/get-home-view-model.ts
  - tests/unit/home-view-model.test.ts
  - tests/e2e/phase1-home-shell.spec.ts
autonomous: true
requirements: [LIBR-07, LIBR-08, TMPL-01]
must_haves:
  truths:
    - "Guest entry renders without an auth gate and prioritizes official NoDoubt Fitness templates on the initial home view."
    - "Signed-in entry renders My Timers before official templates while keeping official templates visible in a separate section."
    - "The shell keeps mobile navigation on shell routes and excludes that chrome from the run route layout."
  artifacts:
    - path: "app/(shell)/page.tsx"
      provides: "Auth-aware home entry that resolves guest versus signed-in content server-side"
      min_lines: 30
    - path: "src/features/home/server/get-home-view-model.ts"
      provides: "Home view-model builder that merges auth context with personal and official timer sources"
      min_lines: 30
    - path: "components/home/home-screen.tsx"
      provides: "Shared mobile home composition for guest and signed-in states"
      min_lines: 40
    - path: "app/run/layout.tsx"
      provides: "Chrome-free run route layout contract"
      contains: "children"
  key_links:
    - from: "app/(shell)/page.tsx"
      to: "src/features/home/server/get-home-view-model.ts"
      via: "server-side home loader import"
      pattern: "getHomeViewModel"
    - from: "src/features/home/server/get-home-view-model.ts"
      to: "src/features/templates/repositories/official-templates.ts"
      via: "official template query"
      pattern: "official-templates"
    - from: "src/features/home/server/get-home-view-model.ts"
      to: "src/features/timers/repositories/personal-timers.ts"
      via: "personal timer query for signed-in state"
      pattern: "personal-timers"
---

<objective>
Finalize the mobile shell and home experience for Phase 1 by consuming the auth bootstrap and data-boundary contracts.

Purpose: Make guest and signed-in entry states visibly different in the right ways while preserving a single branded shell contract.
Output: Route groups, shell layouts, home view-model wiring, signed-in header chip usage, and Phase 1 home coverage.
</objective>

<execution_context>
@C:/Users/Carboteiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Carboteiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-access-and-data-boundaries/01-02-SUMMARY.md
@.planning/phases/01-foundation-access-and-data-boundaries/01-03-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build route groups and shell layouts around the Phase 1 entry contract</name>
  <files>app/layout.tsx, app/(shell)/layout.tsx, app/(shell)/page.tsx, app/(shell)/templates/page.tsx, app/run/layout.tsx, components/shell/mobile-shell.tsx</files>
  <action>Create the root layout and a dedicated shell route group that owns branded mobile chrome, bottom navigation, safe-area spacing, and shared loading states. Keep the run route in its own layout without bottom navigation so later playback work can go full-screen without backtracking. The home route must resolve through a server entry point instead of a client-only auth gate so returning signed-in users render the correct home state on first paint.</action>
  <verify>npm run test:unit -- shell-routes</verify>
  <done>Shell routes exist, the run route has a separate layout contract, and the home page is wired for server-side auth-aware rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Compose guest and signed-in home sections from the shared data and auth adapters</name>
  <files>components/home/home-screen.tsx, components/home/my-timers-section.tsx, components/home/official-templates-section.tsx, components/header/profile-chip.tsx, src/features/home/server/get-home-view-model.ts</files>
  <action>Implement a home view-model builder that consumes the auth context from Plan 01-02 and the template/personal-timer repositories from Plan 01-03. Guests must see official templates first with no empty-state pressure to sign in. Signed-in users must see My Timers first, official templates second, and a first-name header chip sourced from the signed-in identity contract. Use one shared screen composition so the guest/signed-in split stays data-driven rather than branching across multiple pages.</action>
  <verify>npm run test:unit -- home-view-model</verify>
  <done>The home screen shows the correct section ordering for guest and signed-in users, and the signed-in header renders first-name context without changing the route structure.</done>
</task>

<task type="auto">
  <name>Task 3: Add route and home smoke coverage for the Phase 1 shell contract</name>
  <files>tests/unit/home-view-model.test.ts, tests/e2e/phase1-home-shell.spec.ts</files>
  <action>Add fast unit coverage for the guest-versus-signed-in home view-model and a Playwright smoke scenario that proves the shell keeps navigation on shell routes, excludes it from the run layout, and preserves official template visibility across both entry states. Keep the tests focused on Phase 1 contracts only; do not pull in timer-editor or run-engine assumptions from later phases.</action>
  <verify>npm run test:phase1 -- guest-home signed-in-home</verify>
  <done>Phase 1 shell tests fail if section ordering, header identity context, or shell-versus-run chrome rules regress.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:unit -- shell-routes`
- [ ] `npm run test:unit -- home-view-model`
- [ ] `npm run test:phase1 -- guest-home signed-in-home`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Guest and signed-in users share one shell architecture while receiving the correct home prioritization
- The run route is structurally isolated from shell navigation for later full-screen playback work
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-access-and-data-boundaries/01-01-SUMMARY.md`
</output>
